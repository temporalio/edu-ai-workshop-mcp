{
    "image": "temporalio/gitpod-edu:1.4",
    "features": {
        "ghcr.io/devcontainers/features/github-cli:1": {}
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-toolsai.jupyter",
                "ms-python.python"
            ]
        },
        "codespaces": {
            "openFiles": [
                "README.md"
            ]
        }
    },
    "forwardPorts": [8080, 7233, 5125],
    "postCreateCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && . $HOME/.cargo/env && uv sync && uv pip install ipykernel jupyter && uv run python -m ipykernel install --user --name='python3' --display-name='Python 3 (ipykernel)'",
    "postStartCommand": "temporal server start-dev --ui-port 8080 & . $HOME/.cargo/env && cd /workspaces/edu-ai-workshop-mcp && uv run python mcp_servers/weather.py > /tmp/mcp-server.log 2>&1 &",
    "remoteEnv": {
        "PROMPT_DIRTRIM": "2",
        "PATH": "${containerEnv:HOME}/.cargo/bin:${containerEnv:HOME}/.local/bin:${containerEnv:PATH}"
    },
    "portsAttributes": {
        "5125": {
          "label": "MCP Server",
          "visibility": "public"
        }
    },
    "postAttachCommand": "gh codespace ports visibility 5125:public -c $CODESPACE_NAME 2>/dev/null || true"
}